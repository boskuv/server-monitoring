# Default config — disabled until you customize and set LOG_CHECKS_ENABLED=true.
# See log_checks.yaml.example for a full example.

enabled: false

containers: []

# Uncomment to monitor SSH / sudo access attempts (Debian/Ubuntu auth.log):
# host_logs:
#   - name: auth
#     path: /host/var/log/auth.log
#     error_pattern: "(?i)(failed password|invalid user|authentication failure|accepted password|accepted publickey)"
#     extract:
#       - label: ip
#         pattern: "from ([\\d.]+) port"
#       - label: user
#         pattern: "for (?:invalid user )?(\\S+)"
#     max_samples: 3
